export enum SeedMessageEnum {
  SUCCESS = 'The initialization was successful',
  EXIST_ADMIN = 'Exist admin in database',
  CREATE_ADMIN = 'There was no admin in the database and an admin was created',
}
